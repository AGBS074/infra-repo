apiVersion: v1
kind: Service
metadata:
  name: hello-devops-service
  namespace: hello-devops
spec:
  type: NodePort
  selector:
    app: hello-devops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
